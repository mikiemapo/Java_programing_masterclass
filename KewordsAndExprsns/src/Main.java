public class Main {
    public static void main(String [] args) {
<<<<<<< HEAD
       //keyword  | this is the expression except ;and ()|
        double kilometers = (100 * 1.609344) ;
        //keyword int  | this is the expression |
        int highScore = 50;
        //keyword if | this is the expression except for () > and ; |
        if (highScore > 25) {
            highScore = 1000 + highScore; // add bonus points
        }
        //| this is the expression
        int health = 100;
        //keyword if | expression except for operators && outer() > and  |
        if ((health < 25) && (highScore > 1000)) {
            //| this is the assignment expression |
            highScore = highScore - 1000;
        }
    }
}
